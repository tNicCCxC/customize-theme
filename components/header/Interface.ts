import {IBlock} from "../block/Interface";

export interface IHeader {
    listNav     :   string[],
    IBlock      :   IBlock,
    title       :   string,
    logoSrc     :   string,
    cssLogo     :   any,
}